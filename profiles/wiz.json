{
  "schema_version": 2,
  "slug": "wiz",
  "name": "Wiz",
  "enabled": true,
  "careers_url": "https://job-boards.greenhouse.io/wizprivate",
  "resolved_from": "company name 'Wiz' - resolved to Greenhouse board_token 'wizprivate' per the project instructions. Not re-resolved via a fresh web_search in this session.",
  "fetch_type": "api",

  "israel_filter": {
    "method": "post_fetch",
    "structure": "none",
    "param": null,
    "matching_locations": null,
    "ui_actions_structured": [],
    "checked_fields": ["location.name", "offices"],
    "_note": "fetch_type=api on Greenhouse - filtering is a post_fetch check on the API's own location.name AND offices[] (offices is required because Greenhouse frequently reports location.name as 'Multiple Locations')."
  },

  "api": {
    "platform": "greenhouse",
    "endpoint": "https://boards-api.greenhouse.io/v1/boards/wizprivate/jobs?content=true",
    "paginated": false,
    "location_query_param": null,
    "fields": {
      "id": "id",
      "title": "title",
      "location": "location.name",
      "url": "absolute_url"
    }
  },

  "health": {
    "expected_min_jobs": 0,
    "zero_is_plausible": true,
    "sentinel": null,
    "requires_browser": false,
    "_note": "NOT VERIFIED: this environment's network egress does not allow boards-api.greenhouse.io, so no live call was made while building this profile. Placeholder values chosen conservatively so the health gate doesn't misfire on an unknown baseline. Run `python run.py --seed` once, then tighten expected_min_jobs to the observed count and set zero_is_plausible to false."
  },

  "verified_on": "2026-08-11",
  "notes": "Board token 'wizprivate' comes from the project instructions, not from a fresh resolution in this session. Confirm the token is still correct (companies do sometimes migrate Greenhouse tokens) before relying on this profile."
}
